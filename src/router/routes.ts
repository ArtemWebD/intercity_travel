import { lazy } from "react";
import type { IAppRoutes } from "./types/IRoute";

export const routes: IAppRoutes = {
    auth: {
        path: "/auth",
    },
    user: [],
    driver: [],
    agent: [],
};
